<form action="{{ path("fos_user_security_check") }}" method="post" role="form" id="login-form" class="form-inline">
    <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
    {#<p>{{ path(app.request.attributes.get('_route'), app.request.attributes.get('_route_params')) }}</p>#}
    <input type="hidden" name="_target_path" value="{{ currentUrl }}" />

    <div class="form-group">
        <input type="text" id="username" name="_username" value="{{ last_username }}" required="required" class="form-control input-sm" placeholder="Adresse e-mail" />
    </div>
    <div class="form-group">
        <input type="password" id="password" name="_password" required="required" class="form-control input-sm" placeholder="Mot de passe" />
    </div>
    <button type="submit" class="btn btn-primary btn-sm" id="login-button">Connexion</button>
    <div class="clearfix"></div>
    <div class="checkbox">
        <input type="checkbox" id="remember_me" name="_remember_me" value="on"><label for="remember_me"><span class="ui"></span><small>Se souvenir de moi</small></label>
        <span> | <small><a href="{{ path('fos_user_resetting_request') }}">Mot de passe oublié ?</a></small></span>
    </div>
</form>
